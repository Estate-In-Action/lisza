# LISZA runtime dependencies (third-party; stdlib not listed).
# Bank/card XLSX + CSV import, PDF statement parsing, DuckDB analytics,
# Google Sheets export, and the test suite. Pins reflect the versions the
# v2 baseline was validated against.
duckdb==1.5.0
PyMuPDF==1.27.2.3       # imported as `fitz` (statement PDF parsing)
gspread==6.2.1
openpyxl==3.1.5         # XLSX bank/card import (ingest_txns.xlsx_to_csv_text)
pandas==2.3.3
pytest==9.0.3           # test suite
